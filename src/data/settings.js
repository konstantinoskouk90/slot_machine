// Number of wheels and initial color array indexes
const wheelsNum = [...Array(3).keys()], colorsInit = wheelsNum;

// Array of colors
const colorsArray = ["red", "blue", "green", "yellow"];

export {
    wheelsNum,
    colorsInit,
    colorsArray
}